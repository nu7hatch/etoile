#
# Modules disabled (or enabled) in stable vs trunk
#
# Use it to exclude modules not included in stable from the build process.
# Read INSTALL if you want more details.
#

# Frameworks

export distributedview = no
export panekit = no

# Bundles

export etoilebehavior = no

# Private Services

export azalea = no
export azdock = no
export azswitch = no
export busy = no
export system = no

# User Services

export addressmanager = no
export hardware = no
export lookandbehavior = no
